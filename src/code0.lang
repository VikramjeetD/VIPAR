var a:int = 2;
var b:real = 1.23;

func main() {
    for i in 1...100 step 2 {
        var x : int = 12, y : real = 1 if (a == 1) else 0, z : string = weirdFunction(a, b, i);
    }
    var s: string = 'toobadthisstringisjusttoolongomgwhat,nonotagain, stopppppp';
    // Nothing to see here
    /* Useless switch case
       No, really, it's useless. *oh wait that might be useful*/
    switch (i) {
        case 2+3 {
            weirdFunction(a,b,3);
        }
    }
}

func weirdFunction(firstParam: int, secondParam: real, thirdParam: real)->int {
    return 1;
}

























